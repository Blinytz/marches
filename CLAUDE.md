# Relais Claude Code

Les règles communes sont dans `AGENTS.md`. Le chantier Codex courant utilise la
branche `codex/finish-claude-20260723`.

Avant de reprendre :

1. vérifier la branche et `git status` ;
2. lire `docs/CODEX_HANDOFF_20260723.md` ;
3. ne pas modifier simultanément les mêmes fichiers qu'une autre IA ;
4. conserver l'adaptateur Éclats en lecture seule tant qu'aucun registre
   canonique n'est installé ;
5. terminer par les commandes de test et documenter le commit produit.
